import { useEffect, useState } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { ArrowLeft, ArrowRight, Check, ChevronDown, Clock3, Copy, Database, Eye, Flag, Gauge, Layers3, Lock, RefreshCw, ShieldCheck, SlidersHorizontal, Trophy, Users, X } from 'lucide-react';
import { useCampaign } from '../context/CampaignContext';
import { BRANDS, fmtNum } from '../data/campaigns';
import { OWNERS, TIMEZONES } from '../data/validation';
import { Field, Select, TextInput, Toggle } from '../components/builder/form';

type LeaderboardStep = 'type' | 'cadence' | 'setup' | 'filtering' | 'review';

const STEPS: { id: LeaderboardStep; label: string; short: string }[] = [
  { id: 'type', label: 'Campaign type', short: 'Type' },
  { id: 'cadence', label: 'One-time or recurring', short: 'Cadence' },
  { id: 'setup', label: 'General setup', short: 'Setup' },
  { id: 'filtering', label: 'Filtering', short: 'Filtering' },
  { id: 'review', label: 'Review & publish', short: 'Review' },
];

const SCORING = [
  { id: 'turnover', name: 'Bet Amount', note: 'Points equal the sum of qualifying real bets.', formula: 'points = sum(real_bet) × coefficient', example: 'EUR 10 + EUR 25 + EUR 15 = 50 points' },
  { id: 'bet_count', name: 'Bet Count', note: 'Every qualifying bet contributes one point.', formula: 'points = qualifying bet count', example: '12 qualifying bets = 12 points' },
  { id: 'net_win', name: 'Win Amount', note: 'Only positive net wins contribute to the score.', formula: 'points += max(0, win_amount - real_bet)', example: 'EUR 20 win on EUR 5 bet = 15 points' },
];

const SEGMENTS = ['All eligible players', 'High rollers', 'New depositors', 'Reactivated', 'Dormant 30d'];
const FREQUENCIES = ['Minutely', 'Hourly', 'Daily', 'Weekly', 'Monthly', 'Quarterly'];
const CONTENT_LEVELS = ['Vertical', 'Provider', 'Studio', 'Game Type', 'Theme', 'Game ID'];

function read(draft: ReturnType<typeof useCampaign>['draft'], key: string, fallback = ''): string {
  const value = draft.module[key];
  return typeof value === 'string' ? value : fallback;
}

function bool(draft: ReturnType<typeof useCampaign>['draft'], key: string, fallback = false): boolean {
  const value = draft.module[key];
  return typeof value === 'boolean' ? value : fallback;
}

export default function LeaderboardBuilder() {
  const { step: param } = useParams<{ step: string }>();
  const navigate = useNavigate();
  const { draft } = useCampaign();
  const step = (STEPS.find((item) => item.id === param)?.id ?? 'type') as LeaderboardStep;
  const index = STEPS.findIndex((item) => item.id === step);

  useEffect(() => {
    if (draft.type !== 'race') navigate('/create');
  }, [draft.type, navigate]);

  const content: Record<LeaderboardStep, React.ReactNode> = {
    type: <TypeStep />,
    cadence: <CadenceStep />,
    setup: <SetupStep />,
    filtering: <FilteringStep />,
    review: <ReviewStep />,
  };
  const previous = index > 0 ? STEPS[index - 1] : null;
  const next = index < STEPS.length - 1 ? STEPS[index + 1] : null;

  return (
    <div className="flex min-h-full flex-col">
      <header className="sticky top-0 z-20 border-b" style={{ background: 'var(--surface-1)', borderColor: 'var(--border-subtle)' }}>
        <div className="mx-auto w-full max-w-[1240px] px-8">
          <div className="flex items-center justify-between gap-4 pt-4">
            <div className="flex items-center gap-3"><button onClick={() => navigate('/create')} className="flex items-center gap-1.5 text-[13px] font-medium text-fg-secondary"><ArrowLeft size={15} />Campaign type</button><span className="text-fg-muted">/</span><h1 className="text-[15px] font-semibold tracking-tight">New leaderboard</h1></div>
            <button onClick={() => navigate('/')} className="flex h-7 w-7 items-center justify-center rounded-md text-fg-muted"><X size={16} /></button>
          </div>
          <div className="flex items-center py-4">{STEPS.map((item, itemIndex) => <div key={item.id} className="flex items-center" style={{ flex: itemIndex === STEPS.length - 1 ? '0 0 auto' : '1 1 0' }}><button onClick={() => navigate(`/leaderboard/${item.id}`)} className="flex items-center gap-2"><span className="flex h-6 w-6 items-center justify-center rounded-full text-[11px] font-semibold" style={item.id === step ? { background: 'var(--accent)', color: 'var(--accent-fg)' } : { background: 'var(--surface-3)', color: 'var(--fg-secondary)' }}>{itemIndex + 1}</span><span className="whitespace-nowrap text-[12.5px] font-medium" style={{ color: item.id === step ? 'var(--fg-primary)' : 'var(--fg-secondary)' }}>{item.short}</span></button>{itemIndex < STEPS.length - 1 && <div className="mx-3 h-px flex-1" style={{ background: 'var(--border-strong)' }} />}</div>)}</div>
        </div>
      </header>
      <div className="mx-auto flex w-full max-w-[1240px] flex-1 gap-6 px-8 py-6"><main className="min-w-0 flex-1">{content[step]}</main><LeaderboardRail /></div>
      <footer className="sticky bottom-0 z-20 border-t" style={{ background: 'var(--surface-1)', borderColor: 'var(--border-subtle)' }}><div className="mx-auto flex w-full max-w-[1240px] items-center justify-between px-8 py-3.5"><button disabled={!previous} onClick={() => previous && navigate(`/leaderboard/${previous.id}`)} className="flex items-center gap-1.5 rounded-md border px-3 py-2 text-[13px] font-medium" style={previous ? { borderColor: 'var(--border-strong)', color: 'var(--fg-secondary)' } : { borderColor: 'var(--border-subtle)', color: 'var(--fg-muted)' }}><ArrowLeft size={15} />{previous?.short ?? 'Back'}</button><div className="flex items-center gap-3"><span className="text-[12px] text-fg-muted">Step {index + 1} of {STEPS.length}</span><button onClick={() => navigate('/')} className="rounded-md px-3 py-2 text-[13px] font-medium text-fg-secondary">Save draft</button>{next ? <button onClick={() => navigate(`/leaderboard/${next.id}`)} className="flex items-center gap-1.5 rounded-md px-4 py-2 text-[13px] font-semibold" style={{ background: 'var(--accent)', color: 'var(--accent-fg)' }}>Continue<ArrowRight size={15} /></button> : <button onClick={() => navigate('/monitoring')} className="flex items-center gap-1.5 rounded-md px-4 py-2 text-[13px] font-semibold" style={{ background: 'var(--accent)', color: 'var(--accent-fg)' }}>Publish leaderboard<ArrowRight size={15} /></button>}</div></div></footer>
    </div>
  );
}

function TypeStep() {
  const { draft, setModuleField, update } = useCampaign();
  const scoring = read(draft, 'leaderboardScoring', 'turnover');
  const clone = read(draft, 'leaderboardClone');
  const active = SCORING.find((item) => item.id === scoring) ?? SCORING[0];
  return <div className="flex flex-col gap-5"><PageTitle title="Campaign type & duplicate" desc="Choose how players score. You can start from a prior leaderboard, but awards stay outside this flow." /><section className="overflow-hidden rounded-xl border" style={{ borderColor: 'var(--border-subtle)', background: 'var(--surface-1)' }}><div className="border-b px-5 py-4" style={{ borderColor: 'var(--border-subtle)' }}><h3 className="text-[14px] font-semibold text-fg-primary">Scoring type</h3><p className="mt-0.5 text-[12.5px] text-fg-secondary">Only these three scoring types are in the current build.</p></div><div className="divide-y" style={{ borderColor: 'var(--border-subtle)' }}>{SCORING.map((item) => <button key={item.id} onClick={() => setModuleField('leaderboardScoring', item.id)} className="grid w-full grid-cols-[24px_1fr_auto] items-center gap-3 px-5 py-4 text-left" style={{ background: scoring === item.id ? 'var(--accent-bg)' : 'var(--surface-1)' }}><span className="flex h-4 w-4 items-center justify-center rounded-full" style={scoring === item.id ? { background: 'var(--accent)', color: 'var(--accent-fg)' } : { border: '1px solid var(--border-strong)' }}>{scoring === item.id && <Check size={11} strokeWidth={3} />}</span><span><span className="block text-[13px] font-semibold text-fg-primary">{item.name}</span><span className="mt-0.5 block text-[12px] text-fg-secondary">{item.note}</span></span><code className="rounded-md px-2 py-1 text-[11px] text-fg-secondary" style={{ background: 'var(--surface-3)' }}>{item.formula}</code></button>)}</div></section><section className="rounded-xl border px-5 py-4" style={{ borderColor: 'var(--border-subtle)', background: 'var(--surface-1)' }}><div className="flex items-start justify-between gap-5"><div><div className="flex items-center gap-2 text-[14px] font-semibold text-fg-primary"><Copy size={16} style={{ color: 'var(--accent)' }} />Duplicate an existing leaderboard</div><p className="mt-1 text-[12px] text-fg-secondary">Copy configuration as a starting point. Past scores, standings and award distribution do not carry over.</p></div><select value={clone} onChange={(event) => { setModuleField('leaderboardClone', event.target.value); if (event.target.value) update({ name: `Copy of ${event.target.value}` }); }} className="rounded-md border px-3 py-2 text-[12.5px]" style={{ borderColor: 'var(--border-strong)', background: 'var(--surface-2)', color: 'var(--fg-primary)' }}><option value="">Start new</option><option value="Slots Weekly Leaderboard">Slots Weekly Leaderboard</option><option value="Summer Turnover Sprint">Summer Turnover Sprint</option><option value="VIP Net Win Cup">VIP Net Win Cup</option></select></div></section><FormulaPanel formula={active.formula} example={active.example} /></div>;
}

function CadenceStep() {
  const { draft, setModuleField, update } = useCampaign();
  const mode = read(draft, 'leaderboardCadence', 'one_time');
  const recurring = mode === 'recurring';
  return <div className="flex flex-col gap-5"><PageTitle title="One-time or recurring" desc="A single tournament has one finish. Each recurring occurrence scores and ranks independently." /><div className="grid grid-cols-2 gap-4"><Choice active={!recurring} onClick={() => setModuleField('leaderboardCadence', 'one_time')} icon={Flag} title="One-time" desc="One start and end date. Ends when that tournament finishes." /><Choice active={recurring} onClick={() => setModuleField('leaderboardCadence', 'recurring')} icon={RefreshCw} title="Recurring" desc="Repeats on a cadence. Each occurrence is an independent leaderboard." /></div>{recurring ? <section className="rounded-xl border p-5" style={{ borderColor: 'var(--border-subtle)', background: 'var(--surface-1)' }}><div className="grid grid-cols-2 gap-4"><Field label="Frequency" required><Select value={read(draft, 'leaderboardFrequency', 'Daily')} onChange={(value) => setModuleField('leaderboardFrequency', value)} options={FREQUENCIES} /></Field><Field label="Duration per occurrence" hint="minimum 15 minutes" required><TextInput value={read(draft, 'leaderboardDuration', '24 hours')} onChange={(value) => setModuleField('leaderboardDuration', value)} placeholder="e.g. 24 hours" /></Field></div><p className="mt-4 text-[12px] leading-5 text-fg-secondary">No occurrence rolls into a parent leaderboard. Connected leaderboards are explicitly out of scope.</p></section> : <section className="rounded-xl border p-5" style={{ borderColor: 'var(--border-subtle)', background: 'var(--surface-1)' }}><div className="grid grid-cols-2 gap-4"><Field label="Start" hint="UTC, at least 5 minutes from now" required><DateTime valueDate={draft.startDate} valueTime={draft.startTime} onDate={(value) => update({ startDate: value })} onTime={(value) => update({ startTime: value })} /></Field><Field label="End" hint="minimum duration 15 minutes" required><DateTime valueDate={draft.endDate} valueTime={draft.endTime} onDate={(value) => update({ endDate: value })} onTime={(value) => update({ endTime: value })} /></Field></div><p className="mt-4 text-[12px] text-fg-secondary">Maximum one-time duration: 365 days. Times are stored in the selected campaign timezone.</p></section>}</div>;
}

function SetupStep() {
  const { draft, setModuleField, update } = useCampaign();
  const optIn = read(draft, 'leaderboardOptIn', 'automatic');
  const audienceMode = read(draft, 'leaderboardAudienceMode', 'all');
  const toggleBrand = (code: string) => update({ brands: draft.brands.includes(code) ? draft.brands.filter((item) => item !== code) : [...draft.brands, code] });
  return <div className="flex flex-col gap-5"><PageTitle title="General setup" desc="Define the campaign identity, brands, entry behaviour, currencies and audience." /><ConfigSection icon={Flag} title="Campaign identity"><div className="grid grid-cols-2 gap-4"><Field label="Leaderboard name" required><TextInput value={draft.name} onChange={(value) => update({ name: value })} placeholder="e.g. July Slots Turnover" /></Field><Field label="Internal ID" hint="system tracking ID" required><TextInput value={read(draft, 'leaderboardId')} onChange={(value) => setModuleField('leaderboardId', value)} placeholder="e.g. LB-2026-07-001" mono /></Field><Field label="Timezone"><Select value={draft.timezone} onChange={(value) => update({ timezone: value })} options={TIMEZONES} /></Field><Field label="Owner"><Select value={draft.owner} onChange={(value) => update({ owner: value })} options={OWNERS} /></Field></div></ConfigSection><ConfigSection icon={Layers3} title="Brands"><div className="flex flex-wrap gap-2">{BRANDS.map((brand) => { const selected = draft.brands.includes(brand.code); return <button key={brand.code} onClick={() => toggleBrand(brand.code)} className="rounded-md border px-3 py-2 text-left" style={selected ? { borderColor: 'var(--accent-border)', background: 'var(--accent-bg)' } : { borderColor: 'var(--border-subtle)', background: 'var(--surface-2)' }}><span className="font-mono text-[11px] text-fg-muted">{brand.code}</span><span className="ml-2 text-[12.5px] font-medium text-fg-primary">{brand.name}</span></button>; })}</div></ConfigSection><ConfigSection icon={Users} title="Opt-in & audience"><div className="grid grid-cols-2 gap-4"><Choice active={optIn === 'automatic'} onClick={() => setModuleField('leaderboardOptIn', 'automatic')} icon={Gauge} title="Automatic" desc="Points start accumulating on the first qualifying bet." /><Choice active={optIn === 'manual'} onClick={() => setModuleField('leaderboardOptIn', 'manual')} icon={Users} title="Manual" desc="The player must tap Join before entering." /></div>{optIn === 'manual' && <div className="mt-4"><Toggle checked={bool(draft, 'leaderboardRetroactive')} onChange={(value) => setModuleField('leaderboardRetroactive', value)} label="Count eligible bets placed before joining" desc="Retroactive points apply only within the tournament occurrence." /></div>}<div className="mt-5 grid grid-cols-2 gap-4"><Field label="Audience"><Select value={audienceMode} onChange={(value) => setModuleField('leaderboardAudienceMode', value)} options={['all', 'segment']} /></Field>{audienceMode === 'segment' && <Field label="Platform / CRM segment"><Select value={read(draft, 'leaderboardSegment', 'High rollers')} onChange={(value) => setModuleField('leaderboardSegment', value)} options={SEGMENTS.slice(1)} /></Field>}</div>{audienceMode === 'segment' && <div className="mt-4 grid grid-cols-2 gap-2"><Choice active={read(draft, 'leaderboardSegmentKind', 'dynamic') === 'static'} onClick={() => setModuleField('leaderboardSegmentKind', 'static')} icon={Lock} title="Static list" desc="Manual player or segment IDs." /><Choice active={read(draft, 'leaderboardSegmentKind', 'dynamic') === 'dynamic'} onClick={() => setModuleField('leaderboardSegmentKind', 'dynamic')} icon={RefreshCw} title="Dynamic sync" desc="Live membership from Discovery API." /></div>}</ConfigSection><ConfigSection icon={Database} title="Currencies"><div className="grid grid-cols-3 gap-3">{['EUR', 'USD', 'CAD'].map((currency) => <div key={currency} className="rounded-lg border px-3 py-3" style={{ borderColor: 'var(--border-subtle)', background: 'var(--surface-2)' }}><div className="text-[12.5px] font-semibold text-fg-primary">{currency}</div><label className="mt-2 flex items-center gap-2 text-[11.5px] text-fg-secondary">Coefficient <input value={read(draft, `currency_${currency}`, '1')} onChange={(event) => setModuleField(`currency_${currency}`, event.target.value)} className="w-16 rounded border bg-transparent px-2 py-1 font-mono text-[12px]" style={{ borderColor: 'var(--border-strong)', color: 'var(--fg-primary)' }} /></label></div>)}</div></ConfigSection></div>;
}

function FilteringStep() {
  const { draft, setModuleField } = useCampaign();
  const [contentLevel, setContentLevel] = useState(read(draft, 'leaderboardContentLevel', 'Provider'));
  const [contentValue, setContentValue] = useState(read(draft, 'leaderboardContentValue', 'Pragmatic Play'));
  const [contentCoefficient, setContentCoefficient] = useState(read(draft, 'leaderboardContentCoefficient', '1.2'));
  const saveOverride = () => { setModuleField('leaderboardContentLevel', contentLevel); setModuleField('leaderboardContentValue', contentValue); setModuleField('leaderboardContentCoefficient', contentCoefficient); };
  return <div className="flex flex-col gap-5"><PageTitle title="Filtering" desc="Choose which bets count and how much each qualifying bet contributes to the score." /><ConfigSection icon={SlidersHorizontal} title="Qualification"><div className="grid grid-cols-2 gap-4"><Field label="Minimum bet"><TextInput value={read(draft, 'leaderboardMinBet')} onChange={(value) => setModuleField('leaderboardMinBet', value)} placeholder="No minimum" prefix={draft.currency === 'EUR' ? 'EUR' : draft.currency} /></Field><Field label="Maximum bet"><TextInput value={read(draft, 'leaderboardMaxBet')} onChange={(value) => setModuleField('leaderboardMaxBet', value)} placeholder="No maximum" prefix={draft.currency === 'EUR' ? 'EUR' : draft.currency} /></Field></div><div className="mt-4 flex items-center gap-2 rounded-lg border px-3 py-2.5 text-[12px] text-fg-secondary" style={{ borderColor: 'var(--border-subtle)', background: 'var(--surface-2)' }}><ShieldCheck size={14} style={{ color: 'var(--success)' }} />Void and cancelled bets are excluded automatically. This is not a campaign setting.</div></ConfigSection><ConfigSection icon={Gauge} title="Weighting coefficients" desc="All real/bonus-bet and currency coefficients apply together. The most-specific content override wins; content overrides never stack."><div className="grid grid-cols-2 gap-4"><Field label="Real bet coefficient"><TextInput value={read(draft, 'leaderboardRealCoefficient', '1')} onChange={(value) => setModuleField('leaderboardRealCoefficient', value)} /></Field><Field label="Bonus bet coefficient"><TextInput value={read(draft, 'leaderboardBonusCoefficient', '1')} onChange={(value) => setModuleField('leaderboardBonusCoefficient', value)} /></Field></div><div className="mt-5 border-t pt-5" style={{ borderColor: 'var(--border-subtle)' }}><div className="mb-3 flex items-center justify-between"><div><div className="text-[12.5px] font-semibold text-fg-primary">Content override</div><p className="mt-0.5 text-[11.5px] text-fg-secondary">Specificity: {CONTENT_LEVELS.join(' → ')}</p></div><span className="rounded px-2 py-1 text-[10.5px] font-semibold" style={{ background: 'var(--accent-bg)', color: 'var(--accent)' }}>Most specific wins</span></div><div className="grid grid-cols-[150px_1fr_110px] gap-3"><Select value={contentLevel} onChange={setContentLevel} options={CONTENT_LEVELS} /><TextInput value={contentValue} onChange={setContentValue} placeholder="e.g. Pragmatic Play" /><TextInput value={contentCoefficient} onChange={setContentCoefficient} /></div><button onClick={saveOverride} className="mt-3 rounded-md border px-3 py-2 text-[12px] font-medium text-fg-secondary" style={{ borderColor: 'var(--border-strong)' }}>Apply override</button></div></ConfigSection><ConfigSection icon={Trophy} title="Scoring safeguards"><div className="grid grid-cols-2 gap-4"><Field label="Score cap per bet / round" hint="optional"><TextInput value={read(draft, 'leaderboardScoreCap')} onChange={(value) => setModuleField('leaderboardScoreCap', value)} placeholder="No cap" /></Field><Field label="Ranking tie-break"><Select value={read(draft, 'leaderboardTieBreak', 'Earliest to reach score')} onChange={(value) => setModuleField('leaderboardTieBreak', value)} options={['Earliest to reach score']} /></Field></div></ConfigSection><FormulaPanel formula="points from a bet = (real_bet × real coefficient + bonus_bet × bonus coefficient) × currency coefficient × content coefficient" example="For content, only the single most specific matching override applies." /></div>;
}

function ReviewStep() {
  const { draft } = useCampaign();
  const scoring = SCORING.find((item) => item.id === read(draft, 'leaderboardScoring', 'turnover')) ?? SCORING[0];
  const recurring = read(draft, 'leaderboardCadence', 'one_time') === 'recurring';
  const audience = read(draft, 'leaderboardAudienceMode', 'all') === 'segment' ? read(draft, 'leaderboardSegment', 'Platform segment') : 'All eligible players';
  const rows = [['Scoring', scoring.name], ['Schedule', recurring ? `${read(draft, 'leaderboardFrequency', 'Daily')} · ${read(draft, 'leaderboardDuration', '24 hours')} per occurrence` : `${draft.startDate || 'Start not set'} ${draft.startTime} → ${draft.endDate || 'End not set'} ${draft.endTime}`], ['Brands', draft.brands.length ? draft.brands.join(' · ') : 'No brands selected'], ['Opt-in', read(draft, 'leaderboardOptIn', 'automatic') === 'manual' ? `Manual${bool(draft, 'leaderboardRetroactive') ? ' · retroactive points' : ''}` : 'Automatic'], ['Audience', audience], ['Bet range', `${read(draft, 'leaderboardMinBet', 'No min')} to ${read(draft, 'leaderboardMaxBet', 'No max')}`], ['Tie-break', read(draft, 'leaderboardTieBreak', 'Earliest to reach score')]];
  return <div className="flex flex-col gap-5"><PageTitle title="Review & publish" desc="Confirm scoring, occurrence, setup and qualification before the leaderboard becomes active." /><section className="overflow-hidden rounded-xl border" style={{ borderColor: 'var(--border-subtle)', background: 'var(--surface-1)' }}><div className="border-b px-5 py-4" style={{ borderColor: 'var(--border-subtle)' }}><h3 className="text-[14px] font-semibold text-fg-primary">Leaderboard configuration</h3></div><div className="divide-y" style={{ borderColor: 'var(--border-subtle)' }}>{rows.map(([label, value]) => <div key={label} className="grid grid-cols-[180px_1fr] gap-4 px-5 py-3"><span className="text-[11px] font-semibold uppercase tracking-wide text-fg-muted">{label}</span><span className="text-[12.5px] font-medium text-fg-primary">{value}</span></div>)}</div></section><FormulaPanel formula={scoring.formula} example="Standings are monitored throughout the active occurrence. Award/prize distribution is a separate MonoPulse Award System and is not configured here." /><div className="flex items-start gap-3 rounded-lg border px-4 py-3" style={{ borderColor: 'var(--warning)', background: 'var(--warning-bg)' }}><Eye size={16} className="mt-0.5" style={{ color: 'var(--warning)' }} /><p className="text-[12px] leading-5 text-fg-secondary">After publish, operators monitor participation, current standings, opt-in rate and campaign status. Prize settlement is intentionally out of scope for this flow.</p></div></div>;
}

function LeaderboardRail() {
  const { draft } = useCampaign();
  const scoring = SCORING.find((item) => item.id === read(draft, 'leaderboardScoring', 'turnover')) ?? SCORING[0];
  const rows = [['Scoring', scoring.name], ['Occurrence', read(draft, 'leaderboardCadence', 'one_time') === 'recurring' ? read(draft, 'leaderboardFrequency', 'Daily') : 'One-time'], ['Brands', draft.brands.length ? draft.brands.join(' · ') : 'Not set'], ['Audience', read(draft, 'leaderboardAudienceMode', 'all') === 'segment' ? read(draft, 'leaderboardSegment', 'Segment') : 'All players'], ['Filters', read(draft, 'leaderboardMinBet') || read(draft, 'leaderboardMaxBet') ? 'Bet range set' : 'No bet range'], ['Awards', 'Separate Award System']];
  return <aside className="sticky top-6 h-fit w-[300px] shrink-0 overflow-hidden rounded-xl border" style={{ borderColor: 'var(--border-subtle)', background: 'var(--surface-1)' }}><div className="border-b px-4 py-3.5" style={{ borderColor: 'var(--border-subtle)' }}><div className="text-[10px] font-semibold uppercase tracking-wider text-fg-muted">Leaderboard summary</div><div className="mt-1.5 flex items-center gap-2"><span className="flex h-7 w-7 items-center justify-center rounded-md" style={{ background: 'var(--surface-3)', color: 'var(--accent)' }}><Trophy size={15} /></span><div className="truncate text-[13.5px] font-semibold text-fg-primary">{draft.name || 'Untitled leaderboard'}</div></div></div><div className="divide-y" style={{ borderColor: 'var(--border-subtle)' }}>{rows.map(([label, value]) => <div key={label} className="px-4 py-2.5"><div className="text-[10px] font-semibold uppercase tracking-wide text-fg-muted">{label}</div><div className="mt-0.5 truncate text-[12.5px] font-medium text-fg-primary">{value}</div></div>)}</div></aside>;
}

function PageTitle({ title, desc }: { title: string; desc: string }) { return <div><h2 className="text-[16px] font-semibold tracking-tight">{title}</h2><p className="mt-1 text-[13px] text-fg-secondary">{desc}</p></div>; }
function ConfigSection({ icon: Icon, title, desc, children }: { icon: typeof Flag; title: string; desc?: string; children: React.ReactNode }) { return <section className="rounded-xl border" style={{ borderColor: 'var(--border-subtle)', background: 'var(--surface-1)' }}><div className="flex items-start gap-3 border-b px-5 py-3.5" style={{ borderColor: 'var(--border-subtle)' }}><span className="flex h-8 w-8 shrink-0 items-center justify-center rounded-lg" style={{ background: 'var(--surface-3)', color: 'var(--fg-secondary)' }}><Icon size={16} /></span><div><h3 className="text-[14px] font-semibold text-fg-primary">{title}</h3>{desc && <p className="mt-0.5 text-[12px] leading-5 text-fg-secondary">{desc}</p>}</div></div><div className="p-5">{children}</div></section>; }
function Choice({ active, onClick, icon: Icon, title, desc }: { active: boolean; onClick: () => void; icon: typeof Flag; title: string; desc: string }) { return <button onClick={onClick} className="flex items-start gap-3 rounded-lg border p-4 text-left" style={active ? { borderColor: 'var(--accent-border)', background: 'var(--accent-bg)' } : { borderColor: 'var(--border-subtle)', background: 'var(--surface-2)' }}><span className="flex h-8 w-8 shrink-0 items-center justify-center rounded-md" style={{ background: active ? 'var(--accent)' : 'var(--surface-3)', color: active ? 'var(--accent-fg)' : 'var(--fg-secondary)' }}><Icon size={16} /></span><span><span className="block text-[13px] font-semibold text-fg-primary">{title}</span><span className="mt-1 block text-[11.5px] leading-5 text-fg-secondary">{desc}</span></span></button>; }
function FormulaPanel({ formula, example }: { formula: string; example: string }) { return <section className="rounded-xl border px-5 py-4" style={{ borderColor: 'var(--accent-border)', background: 'var(--accent-bg)' }}><div className="flex items-center gap-2 text-[12.5px] font-semibold text-fg-primary"><Gauge size={15} style={{ color: 'var(--accent)' }} />Scoring formula</div><code className="mt-3 block overflow-x-auto rounded-md px-3 py-2.5 text-[12px] text-fg-primary" style={{ background: 'var(--surface-1)' }}>{formula}</code><p className="mt-2 text-[12px] leading-5 text-fg-secondary">{example}</p></section>; }
function DateTime({ valueDate, valueTime, onDate, onTime }: { valueDate: string; valueTime: string; onDate: (value: string) => void; onTime: (value: string) => void }) { return <div className="grid grid-cols-[1fr_92px] gap-2"><input type="date" value={valueDate} onChange={(event) => onDate(event.target.value)} className="rounded-md border px-3 py-2 text-[13px]" style={{ borderColor: 'var(--border-strong)', background: 'var(--surface-2)', color: 'var(--fg-primary)' }} /><input type="time" value={valueTime} onChange={(event) => onTime(event.target.value)} className="rounded-md border px-3 py-2 text-[13px]" style={{ borderColor: 'var(--border-strong)', background: 'var(--surface-2)', color: 'var(--fg-primary)' }} /></div>; }
